---
description: Show your personal quota attainment scorecard — bookings vs target, pipeline coverage, and pacing.
argument-hint: "[period | quota amount | deals to include …]"
---

Apply the `sales-reporting` skill to: $ARGUMENTS

Scope notes:
- Self-scoped for any rep: runs sales-reporting in rep-scorecard/attainment mode for the owner only.
- Read-only; no CRM writes occur.
- For team or org-wide reporting, RevOps should use `/report` instead.
