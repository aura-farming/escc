---
description: A-Z research + a sequenced, multi-channel plan of attack for ONE named target account.
argument-hint: "<account name or domain> [what we sell / context]"
---

Apply the `account-attack-plan` skill to: $ARGUMENTS

Scope notes:
- ONE named target account — for a list/segment use `/prospect`; for a brief only use `/research`; for an already-open deal use `/close-plan`.
- Orchestrates the read-only research + planning agents (`account-researcher`, `prospect-researcher`, `competitor-analyst`, `warm-path-mapper`, `signal-scorer`, `sales-planner`) and returns a brief PLUS a sequenced plan of attack.
- Screen do-not-contact / contactability FIRST; every touch is draft-only behind the fail-closed send-gate; every claim is sourced.
