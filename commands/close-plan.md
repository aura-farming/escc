---
description: Build a backward date-plan from target signature through every enterprise gate.
argument-hint: "[deal | target close date | stage …]"
---

Apply the `close-plan` skill to: $ARGUMENTS

Scope notes:
- Date-plan is built backward from the agreed close date through legal, security, procurement, and exec approval gates.
- References `mutual-action-plan` for buyer-owned milestones and `paper-process` for document timelines — does not duplicate them.
- Slippage risks and critical-path items are surfaced explicitly; forecast impact is advisory, not automatic.
