---
description: Draft a first-touch cold message that passes the quality gate — real personalization, one ask, concrete proof, zero filler.
argument-hint: "<prospect / account + the signal you're acting on>"
---

Apply the `cold-outreach` skill to: $ARGUMENTS

Scope notes:
- Personalization must come from a **real signal** (highest source-priority first), never generic praise or a soft close.
- Proof comes from `product-knowledge` (approved + provenance) — never invent a metric or customer reference.
- Output is **draft-only** and handed to `outbound-reviewer` before the send gate.
