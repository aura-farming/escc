---
description: Compose a thread-aware follow-up, breakup, or recycle-and-refer — reads the thread first, never re-pitches blind.
argument-hint: "[thread / contact / deal]"
---

Apply the `follow-up-ops` skill to: $ARGUMENTS

Scope notes:
- **Read the full thread first** — never repeat a prior ask or re-pitch blind; route inbound replies to `reply-handling`.
- A review-pack is required before any bulk follow-up; nothing sends without the send gate.
- Logging goes through `crm-operator`; nothing is claimed sent/logged without a tool-result.
