---
description: Roll up the team forecast — commit, best-case, and pipeline coverage with variance notes.
argument-hint: "[period | team | rep | segment …]"
---

Apply the `forecast-rollup` skill to: $ARGUMENTS

Scope notes:
- For managers assembling the team forecast call; for a rep's own commit use `/commit` instead.
- Read-only aggregation; forecast submissions go through `crm-operator`.
- Output includes coverage ratio, risk-weighted commit, and deals to watch.
