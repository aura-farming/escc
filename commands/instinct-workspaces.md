---
description: List available instinct workspaces and switch the active one.
argument-hint: "[workspace name | list | switch <name>]"
---

Apply the `instincts` skill to: $ARGUMENTS

Scope notes:
- Lists all workspaces (personal, team, shared) with instinct counts and last-modified dates.
- Switch sets the active workspace for all subsequent instinct commands in this session.
- Workspace isolation is enforced — personal instincts do not bleed into team workspaces.
