---
description: Cluster active instincts and draft new skills, commands, or agents from patterns.
argument-hint: "[workspace | tag | cluster hint]"
---

Apply the `instincts` skill to: $ARGUMENTS

Scope notes:
- Clusters approved instincts by theme, then proposes new skill, command, or agent drafts.
- Drafts are written to a staging path for human review before any `npm test` or catalog update.
- Use after a meaningful batch of instincts has been approved via `/instinct-status`.
