interface:
  display_name: "Escc"
  short_description: "Repo-specific patterns and workflows for escc"
  default_prompt: "Use the escc repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true