# Normalize line endings to LF across platforms (CI runs on macOS, Ubuntu, Windows).
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
